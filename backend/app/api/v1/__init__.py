from fastapi import APIRouter

router = APIRouter()

# Import and include your v1 routers here, for example:
# from . import auth as auth_router
# router.include_router(auth_router.router, prefix="/auth", tags=["auth"])
