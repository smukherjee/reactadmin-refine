[pytest]
addopts = -q -m "not functional"
asyncio_mode = auto
markers =
    functional: mark test as functional / integration that requires a running backend or external services
    timeout: mark a test with a timeout value (provided by pytest-timeout)
